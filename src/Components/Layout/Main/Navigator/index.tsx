const Navigator = () => {
  return (
    <nav className="fixed bottom-0 left-1/2 -translate-x-1/2 w-full max-w-[675px] bg-white border-t border-gray-200">
      navigator
    </nav>
  );
};

export default Navigator;
